import styled from "styled-components";

export const AddListaStyle = styled.div`
    button, input{
        background: transparent; border: none;
        color: #FFFFFF; font-size: 20px;
        border-radius: 5px;
        padding: 5px; margin-left: 10px;
    }
    input{
        outline: none;
        width: 89.5%;
    }
`